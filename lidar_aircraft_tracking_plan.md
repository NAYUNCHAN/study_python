# 라이다 기반 비행체 시간 보정 및 추적 계획

## 1. 목표

라이다로 고속 비행체를 탐지할 때 발생하는 다음 문제를 보정한다.

- 거리 차이에 따른 광 전파시간 차이
- 라이다 스캔 중 비행체 이동에 따른 포인트클라우드 왜곡
- 센서 간 시각 불일치
- 클러스터 중심 측정 실패
- 지연 측정값의 시간 정렬 문제

최종 목표는 모든 측정값을 하나의 공통 기준시각으로 정렬하고, 비행체의 위치·속도·가속도를 안정적으로 추정하는 것이다.

---

## 2. 전체 처리 흐름

```text
센서 시각 동기화
→ 포인트별 실제 측정시각 계산
→ 광 전파시간 보정
→ 센서 및 표적 운동 보상
→ 비행체 포인트 클러스터링
→ 클러스터 중심 및 측정값 생성
→ 칼만 필터 상태추정
→ 측정 누락 처리
→ 공통 기준시각 상태 출력
```

---

## 3. 센서 시각 동기화

모든 센서가 동일한 시간 기준을 사용하도록 구성한다.

권장 방식:

1. 하드웨어 트리거
2. GPS/GNSS PPS
3. IEEE 1588 PTP
4. FPGA 또는 센서 내부 하드웨어 타임스탬프

각 포인트에는 최소한 다음 시간 정보를 저장한다.

```text
sensor_id
frame_id
point_id
raw_timestamp
timestamp_type
scan_offset
range
sync_status
```

`timestamp_type`은 송신시각, 수신시각, 프레임 시작시각, 패킷 도착시각 등을 명확히 구분한다.

---

## 4. 광 전파시간 보정

라이다 거리 측정식:

\[
R = rac{c(t_{rx}-t_{tx})}{2}
\]

비행체에 실제로 광이 반사된 시각:

\[
t_{hit} = rac{t_{tx}+t_{rx}}{2}
\]

수신시각을 기준으로 기록한 경우:

\[
t_{hit} = t_{rx} - rac{R}{c}
\]

송신시각을 기준으로 기록한 경우:

\[
t_{hit} = t_{tx} + rac{R}{c}
\]

주의:

- `2R/c`는 송신부터 수신까지의 왕복시간이다.
- 수신시각에서 실제 반사시각으로 보정할 때는 일반적으로 `R/c`를 사용한다.

---

## 5. 포인트클라우드 디스큐

회전식 또는 주사식 라이다는 한 프레임의 모든 포인트를 동시에 측정하지 않는다.

각 포인트의 실제 측정시각을 계산한 뒤, 기준시각 \(t_0\)으로 이동시킨다.

단순 병진운동 모델:

\[
\mathbf{p}(t_0)
=
\mathbf{p}(t_i)
+
\mathbf{v}(t_0-t_i)
+
rac{1}{2}\mathbf{a}(t_0-t_i)^2
\]

센서 자체가 움직이는 경우에는 GNSS/IMU 자세정보를 이용해 센서 위치와 자세 변화도 함께 보정한다.

권장 기준시각:

- 포인트클라우드 정렬: 프레임 종료시각
- 센서 융합: 공통 융합시각
- 제어 출력: 현재시각 또는 예상 제어 적용시각

---

## 6. 비행체 포인트 클러스터링

### 처리 순서

```text
배경 제거
→ 포인트 군집화
→ 비행체 후보 판별
→ 대표 측정값 생성
```

### 대표 측정값

```text
centroid
measurement_time
point_count
bounding_box
measurement_covariance
confidence
```

클러스터 중심은 다음과 같이 사용한다.

\[
\mathbf{z}_k =
egin{bmatrix}
x \
y \
z
\end{bmatrix}
\]

이 값을 칼만 필터의 측정값으로 입력한다.

---

## 7. 클러스터 중심을 찾지 못한 경우

운동 보상은 포인트를 정렬해 클러스터 형성을 개선하지만, 클러스터 중심 탐지를 보장하지는 않는다.

중심을 찾지 못할 수 있는 원인:

- 포인트 수 부족
- 표적 가림
- 낮은 반사율
- 배경과 클러스터 병합
- 하나의 표적이 여러 클러스터로 분리
- 잘못된 운동 보상
- 탐지 범위 이탈

중심을 찾지 못한 경우 즉시 시스템 오류로 처리하지 않고 `측정 누락(Missed Detection)`으로 처리한다.

```text
클러스터 중심 있음
→ 칼만 예측
→ 측정 업데이트

클러스터 중심 없음
→ 칼만 예측만 수행
→ 누락 횟수 증가
→ 추적 신뢰도 감소
```

상태는 예측값으로 유지한다.

\[
\hat{\mathbf{x}}_{k|k}
=
\hat{\mathbf{x}}_{k|k-1}
\]

공분산은 증가한다.

\[
\mathbf{P}_{k|k-1}
=
\mathbf{F}\mathbf{P}_{k-1|k-1}\mathbf{F}^{T}
+
\mathbf{Q}
\]

---

## 8. 칼만 필터 상태추정

권장 상태벡터:

\[
\mathbf{x}
=
egin{bmatrix}
x & y & z &
v_x & v_y & v_z &
a_x & a_y & a_z
\end{bmatrix}^{T}
\]

기본 단계:

1. 이전 상태를 이용해 현재 위치 예측
2. 클러스터 중심이 있으면 측정 업데이트
3. 클러스터 중심이 없으면 예측만 유지
4. 공분산과 추적 신뢰도 갱신

고속 비행체 또는 급기동 표적에는 다음 모델을 조합할 수 있다.

- CV: 등속도 모델
- CA: 등가속도 모델
- CT: 선회 모델
- IMM-EKF 또는 IMM-UKF

---

## 9. 게이팅 기반 클러스터 탐색

추적이 시작된 뒤에는 칼만 필터의 예측 위치를 클러스터링에 다시 활용한다.

```text
칼만 필터 예측 위치
→ 예상 탐색영역 설정
→ 해당 영역 내 클러스터 탐색
→ 가장 적합한 클러스터 연결
```

이를 게이팅이라고 한다.

예측 위치와 클러스터 중심 사이의 마할라노비스 거리를 사용할 수 있다.

\[
d^2
=
(\mathbf{z}-\hat{\mathbf{z}})^T
\mathbf{S}^{-1}
(\mathbf{z}-\hat{\mathbf{z}})
\]

게이트 범위를 벗어난 클러스터는 현재 트랙과 연결하지 않는다.

---

## 10. 측정 누락 및 트랙 관리

예시 정책:

```text
1~2회 누락
→ 예측만 수행
→ 트랙 유지

3~5회 누락
→ 신뢰도 감소
→ 탐색영역 확대

임계 횟수 초과
→ 트랙 소실 판정
→ 추적 종료 또는 재탐색
```

고정 횟수보다 누락 지속시간으로 관리하는 것이 더 적절할 수 있다.

\[
T_{miss}
=
N_{miss}
	imes
T_{frame}
\]

트랙 삭제 조건은 다음을 함께 고려한다.

- 연속 누락 횟수
- 마지막 측정 이후 경과시간
- 공분산 크기
- 탐지 영역 이탈 여부
- 추적 신뢰도

---

## 11. 지연 측정값 처리

다음 세 시각을 구분한다.

```text
measurement_time
arrival_time
processing_time
```

과거 시각의 측정값이 늦게 도착하면 OOSM 방식으로 처리한다.

```text
과거 상태 복원
→ 해당 시각에서 측정 업데이트
→ 이후 상태 재예측
→ 현재시각 상태 복구
```

이를 위해 최근 상태와 측정값을 순환 버퍼에 저장한다.

---

## 12. 구현 우선순위

### 1단계: 최소 구현

- 센서 타임스탬프 기준 확인
- 포인트별 측정시각 계산
- 광 전파시간 보정
- 기본 클러스터링
- 클러스터 중심 계산
- 등가속도 칼만 필터
- 측정 누락 처리

### 2단계: 정확도 개선

- 포인트별 디스큐
- 게이팅 기반 클러스터 연결
- 측정 공분산 동적 계산
- 트랙 신뢰도 관리
- 센서 움직임 보상

### 3단계: 고급 추적

- IMM-EKF 또는 IMM-UKF
- OOSM 재처리
- 다중 표적 추적
- 레이더·카메라·라이다 융합
- 비행체 회전운동 보상

---

## 13. 핵심 결론

```text
운동 보상은 클러스터링 성공률을 높이는 전처리이다.
클러스터 중심을 항상 보장하는 기능은 아니다.
```

클러스터 중심이 없을 때는 오류로 즉시 종료하지 않고:

```text
측정 누락 처리
→ 칼만 필터 예측 유지
→ 공분산 증가
→ 연속 누락 횟수 관리
→ 임계값 초과 시 트랙 종료
```

구조를 사용한다.

최종 권장 구조:

```text
시간 동기화
→ 광 전파시간 보정
→ 포인트 디스큐
→ 클러스터링
→ 중심 계산
→ 칼만 필터 업데이트
→ 중심 미검출 시 예측만 유지
→ 연속 미검출 시 트랙 종료
```
